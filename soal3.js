let max = 20;
for (let index = 0; index < max; index++) {
    if(index % 2 == 0) {
        document.write(`nilai ke ${index} : genap </br>`)
    } else {
        document.write(`nilai ke ${index} : ganjil </br>`)
    }
    
};